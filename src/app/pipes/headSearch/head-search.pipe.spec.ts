import { HeadSearchPipe } from './head-search.pipe';

describe('HeadSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new HeadSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
